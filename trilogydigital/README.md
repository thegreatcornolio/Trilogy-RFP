# Trilogy Digital Proposal

Static HTML recreation of [v0-trilogydigital.vercel.app](https://v0-trilogydigital.vercel.app).

## View locally

Open `index.html` in a browser, or serve the folder:

```bash
python3 -m http.server 8080 --directory .
```

Then visit `http://localhost:8080`.
