Drop official logo files here. Do not invent or recreate logos.

**Trilogy (all decks)** — put the file in this folder:

`presentations/_shared/logos/trilogy.png`  
(or `.svg` / `.webp` / `.jpg`)

**This deck — Medscheme & Precision Point** — put files in:

`presentations/medscheme-international-proposal/assets/logos/`

| File name | Brand |
| --- | --- |
| `medscheme.png` (or `.svg`) | Medscheme |
| `precision-point.png` (or `.svg`) | Precision Point |

Transparent PNG or SVG works best on the dark navy slides.

After you drop the files, the deck picks them up automatically. No need to edit HTML.
